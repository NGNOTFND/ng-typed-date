import { NgModelDateDirective } from './ng-model-date.directive';

describe('NgModelDateDirective', () => {
  it('should create an instance', () => {
    const directive = new NgModelDateDirective();
    expect(directive).toBeTruthy();
  });
});
